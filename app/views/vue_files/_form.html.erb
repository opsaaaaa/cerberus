<%= form_with(model: vue_file, local: true) do |form| %>
  <% if vue_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vue_file.errors.count, "error") %> prohibited this vue_file from being saved:</h2>

      <ul>
        <% vue_file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
